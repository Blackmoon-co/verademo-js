veracode_static_scan:
  artifact_file: app.zip

  push:
    trigger: true
    branches_to_run:
      - '*'

  pull_request:
    trigger: true
    action:
      - opened
      - synchronize
    target_branch:
      - default_branch

  dispatch: true
